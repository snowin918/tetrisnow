// Provides stb_image's implementation in exactly one translation unit,
// per its own documented usage pattern (every other file just includes
// the header declarations).
#define STB_IMAGE_IMPLEMENTATION
#include <stb_image.h>
